# V5 Canonical Evidence Package

This directory preserves the final synthetic, offline evidence chain for the V5 selective-promotion study.

## Final closure

- V5-J paired-contrast predictive viability: **NOT JUSTIFIED**
- Source baseline: **retained**
- Personalized promotion serving policy: **not authorized**
- Final serving model: **not trained**
- Threshold selection, calibration, and confirmation: **not executed**

## Integrity

`SOURCE_FILE_MANIFEST.csv` records every copied canonical input with source path, repository-relative destination, byte count, and SHA-256.
`FILE_MANIFEST.csv` records SHA-256 for every package file except itself, because a file cannot contain a stable hash of its own final bytes.
